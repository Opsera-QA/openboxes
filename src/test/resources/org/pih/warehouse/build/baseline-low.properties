# Fixture baseline: low coverage thresholds that the high-coverage report easily exceeds
# Used to test the "above baseline" (pass) path
coverage.instruction=9.0
coverage.branch=18.0
coverage.tolerance=0.5
